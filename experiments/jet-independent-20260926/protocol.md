# Frozen independent validation of the rule candidate

Candidate fixed before creating this set: anchor2 step875, adapter SHA256 3ad3cc233db094723e875f978bee59de402f8901e0761630e4a9dad14e59f89f, on released Jet v6.2 revision fbc3d2daa679e0d4bd9f99c9912b6496d5a41f0a. No alternate checkpoints are evaluated or selected using these results.

Fresh set: 400 cases each for AND/OR, OR/NOT and conditional rules with ASTs distinct from previous training, selection and test ASTs; 402 date cases in 2045–2055 across three formats; 154 banking cases (two per class), 180 finance cases (60 per sentiment), 246 sarcasm cases (six positive, 240 negative). Total 2,182. Synthetic primitives remain shared with prior generators, so this is not an unseen-domain claim. Counterfactual pairs remain in this one evaluation set. Build verifies every new truth table with a second Boolean oracle and all dates with independent day offsets. Records are excluded by normalized long state text, whole-state hash, and available source-group identifiers against 46 pinned local input files. Finance entities and sarcasm rephrases are grouped. Unknown original backbone pretraining cannot be audited.

The initial target was 320 sarcasm cases with80 positives. Only six unused positive cases survive exclusions. Keep these as exploratory and require at least20 fresh positive cases (the size of earlier focused evaluation panels) before treating this as adequate independent sarcasm evidence. Do not weaken exclusions or reuse old examples under a fresh label. Banking and finance met their full targets. Broad retention additionally uses the previously opened 1,554-case diagnostic split, explicitly labeled reused.

Before any model scores are observed, gates are fixed: each fresh/reused family metric must stay within2percentage points of released v6.2, and the existing objective must improve. Fresh objective is mean accuracy of the four rule/date families; reused objective remains70% mean rule/date accuracy +30% broad retention accuracy. Sarcasm uses positive-class F1; finance macro-F1; other tasks accuracy. Both adapter and merged BF16 weights are evaluated. Report every gate, including the known insufficient fresh sarcasm sample, without automatically publishing.

Merge uses pinned full v6.2 shards plus FP32 B@A delta, rounded into BF16, with all adapter keys consumed. Local output is releases/jet-rules-candidate-20260926, not a new public model. Merge fidelity on all2,182 fresh cases requires at most3% argmax flips and maximum uncalibrated probability difference below0.075. Exercise the shipped loader/API for choice/score/noul and the existing11,495-token probe, with16,384-token boundary enforcement. Calibration remains inherited.

After merging, run the fixed656-case Kev development comparison once. Its scores are diagnostic and do not select checkpoints. Kev comparison figures are previously published references, not local competitor reruns; not the official Decision Index.

Build with .venv/bin/python (PyArrow/tokenizers environment); launch.py with .venv-qwen35/bin/python (Torch environment). code-manifest.json pins executed sources; code-snapshot.json pins shared source Git blobs. Original source revisions for public pools are in ../jet-focused-20260925/data-audit.json and the exact source byte hashes in this data-audit.json. Raw examples, probabilities and model weights stay out of Git. No automatic release or deployment is performed.
