# Retention-anchored rule experiment

See protocol.md for the fixed experiment and data-audit.json for inherited split hashes. Run with the pinned environment using `python launch.py` after restoring the exact data and shared source snapshot. Output paths must not already exist. Model release remains v6.2 until independent validation supports promotion.
